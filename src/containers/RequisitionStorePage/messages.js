import { defineMessages } from "react-intl";

export const scope = "containers.RequisitionStorePage";

export default defineMessages({
  pageTitle: {
    id: `${scope}.pageTitle`,
    defaultMessage: "Requisition Store Page",
  },
});
