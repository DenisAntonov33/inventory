import { defineMessages } from "react-intl";

export const scope = "containers.ProfilePage";

export default defineMessages({
  pageTitle: {
    id: `${scope}.pageTitle`,
    defaultMessage: "Profile",
  },
  languageSelect: {
    id: `${scope}.languageSelect`,
    defaultMessage: "Language",
  },
});
