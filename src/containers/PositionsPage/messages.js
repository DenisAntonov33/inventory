import { defineMessages } from "react-intl";

export const scope = "containers.PositionsPage";

export default defineMessages({
  pageTitle: {
    id: `${scope}.pageTitle`,
    defaultMessage: "Positions Page",
  },
});
