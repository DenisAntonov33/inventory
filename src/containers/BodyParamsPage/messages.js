import { defineMessages } from "react-intl";

export const scope = "containers.BodyParamsPage";

export default defineMessages({
  pageTitle: {
    id: `${scope}.pageTitle`,
    defaultMessage: "Body Params Page",
  },
  openActionTooltip: {
    id: `${scope}.openActionTooltip`,
    defaultMessage: "Open Body Param",
  },
});
