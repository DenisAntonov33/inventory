import { defineMessages } from "react-intl";

export const scope = "containers.RequisitionPage";

export default defineMessages({
  pageTitle: {
    id: `${scope}.pageTitle`,
    defaultMessage: "Requisition Page",
  },
});
