import { defineMessages } from "react-intl";

export const scope = "containers.EmployeesItemPage";

export default defineMessages({
  pageTitle: {
    id: `${scope}.pageTitle`,
    defaultMessage: "Employee Item Page",
  },
  historyTitle: {
    id: `${scope}.historyTitle`,
    defaultMessage: "Employee history",
  },
});
