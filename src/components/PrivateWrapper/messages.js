import { defineMessages } from "react-intl";

export const scope = "components.PrivateWrapper";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "Dashboard",
  },
  logout: {
    id: `${scope}.logout`,
    defaultMessage: "Logout",
  },
});
